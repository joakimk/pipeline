language: ruby
rvm:
  - "1.9.3"
before_script:
  - psql -c 'create database deployer_test;' -U postgres
script: "export TRAVIS=true && bundle exec rake spec:unit && bundle exec rake spec && DB=true bundle exec rake spec"
